# Personal access token (omnara_pat_v1_...) used for every API call.
OMNARA_API_KEY=omnara_pat_v1_...

# X (Twitter) API bearer token. The notebook upserts it as a project secret
# and Omnara injects it on pool machines as X_BEARER_TOKEN; it never appears
# in the agent config.
X_BEARER_TOKEN=

# Optional: Slack app configuration token (xoxe.xoxp-...) from
# https://api.slack.com/apps, used once by the "Connect Slack" cell. These
# tokens expire after ~12 hours; generate a fresh one when you run that cell.
SLACK_APP_CONFIGURATION_TOKEN=

# Omnara API base URL (the notebook's first cell). Hosted Omnara is
# https://api.omnara.com/v1; for self-hosted use the server origin plus
# /api/v1, e.g. http://localhost:8080/api/v1.
