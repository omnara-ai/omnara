name: Release CLI

on:
  push:
    tags:
      - 'cli-v[0-9]+.[0-9]+.[0-9]+'
      - 'cli-v[0-9]+.[0-9]+.[0-9]+-*'

permissions:
  contents: read

concurrency:
  group: cli-release
  cancel-in-progress: false

jobs:
  publish:
    name: Publish omnara to npm
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    permissions:
      contents: read
      id-token: write
    environment:
      name: releases
      url: https://www.npmjs.com/package/omnara
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Validate tag belongs to main
        run: |
          git fetch --no-tags origin +refs/heads/main:refs/remotes/origin/main
          if ! git merge-base --is-ancestor HEAD origin/main; then
            echo "release tag must point to a commit on main"
            exit 1
          fi

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v6
        with:
          node-version-file: .node-version

      - run: corepack enable

      - name: Set version from tag
        working-directory: frontend/packages/cli
        run: npm pkg set version="${GITHUB_REF_NAME#cli-v}"

      - run: pnpm install --frozen-lockfile

      - run: pnpm --filter omnara typecheck

      - run: pnpm --filter omnara test

      - run: pnpm --filter omnara build

      - name: Publish
        working-directory: frontend/packages/cli
        run: |
          version="${GITHUB_REF_NAME#cli-v}"
          case "$version" in
            *-*) dist_tag=beta ;;
            *) dist_tag=latest ;;
          esac
          pnpm publish --no-git-checks --tag "$dist_tag"
