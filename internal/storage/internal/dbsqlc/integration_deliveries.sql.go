// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: integration_deliveries.sql

package dbsqlc

import (
	"context"
	"encoding/json"
	"time"

	"github.com/google/uuid"
)

const cancelUnavailableIntegrationDeliveries = `-- name: CancelUnavailableIntegrationDeliveries :many
WITH sweep_cursor AS MATERIALIZED (
  SELECT last_item_id, cycle_end_id
  FROM integration_sweep_cursors
  WHERE sweep_kind = 'delivery_unavailable'
  FOR UPDATE SKIP LOCKED
), cycle AS MATERIALIZED (
  SELECT cursor.last_item_id,
         coalesce(cursor.cycle_end_id, upper_bound.id) AS cycle_end_id
  FROM sweep_cursor cursor
  LEFT JOIN LATERAL (
    SELECT delivery.id
    FROM integration_deliveries delivery
    WHERE delivery.transport = 'connector'
      AND delivery.state IN ('pending', 'retry_wait')
    ORDER BY delivery.id DESC
    LIMIT 1
  ) upper_bound ON true
), candidates AS MATERIALIZED (
  SELECT candidate.id
  FROM cycle
  CROSS JOIN LATERAL (
    SELECT delivery.id
    FROM integration_deliveries delivery
    WHERE delivery.transport = 'connector'
      AND delivery.state IN ('pending', 'retry_wait')
      AND delivery.id > cycle.last_item_id
      AND delivery.id <= cycle.cycle_end_id
    ORDER BY delivery.id
    FOR UPDATE SKIP LOCKED
    LIMIT $1
  ) candidate
), progress AS MATERIALIZED (
  SELECT count(*) AS candidate_count,
         (SELECT id FROM candidates ORDER BY id DESC LIMIT 1) AS last_candidate_id
  FROM candidates
), advance_cursor AS (
  UPDATE integration_sweep_cursors cursor
  SET last_item_id = CASE
        WHEN progress.candidate_count < $1::integer
          OR progress.last_candidate_id = cycle.cycle_end_id
          THEN '00000000-0000-0000-0000-000000000000'::uuid
        ELSE progress.last_candidate_id
      END,
      cycle_end_id = CASE
        WHEN progress.candidate_count < $1::integer
          OR progress.last_candidate_id = cycle.cycle_end_id
          THEN NULL
        ELSE cycle.cycle_end_id
      END,
      updated_at = statement_timestamp()
  FROM cycle CROSS JOIN progress
  WHERE cursor.sweep_kind = 'delivery_unavailable'
    AND cycle.cycle_end_id IS NOT NULL
  RETURNING cursor.sweep_kind
), unavailable AS MATERIALIZED (
  SELECT candidate.id
  FROM candidates candidate
  JOIN integration_deliveries delivery ON delivery.id = candidate.id
  WHERE NOT EXISTS (
      SELECT 1
      FROM integration_target_bindings binding
      JOIN integration_targets target
        ON target.project_id = binding.project_id
       AND target.id = binding.integration_target_id
       AND target.deleted_at IS NULL
      JOIN integration_installs install
        ON install.project_id = binding.project_id
       AND install.id = binding.integration_install_id
       AND install.state = 'active'
       AND install.deleted_at IS NULL
      JOIN integration_apps app
        ON app.org_id = install.org_id
       AND app.id = install.integration_app_id
       AND app.state = 'active'
       AND app.deleted_at IS NULL
      WHERE binding.project_id = delivery.project_id
        AND binding.id = delivery.integration_target_binding_id
        AND binding.agent_id = delivery.agent_id
        AND binding.integration_target_id = delivery.integration_target_id
        AND binding.send_allowed
        AND binding.revoked_at IS NULL
        AND (
          binding.integration_route_id IS NULL
          OR EXISTS (
            SELECT 1 FROM integration_routes route
            WHERE route.project_id = binding.project_id
              AND route.integration_install_id = binding.integration_install_id
              AND route.id = binding.integration_route_id
              AND route.state = 'active'
              AND route.deleted_at IS NULL
          )
        )
    )
)
UPDATE integration_deliveries delivery
SET state = 'canceled',
    last_error = jsonb_build_object(
      'code', 'channel_unavailable',
      'message', 'channel became unavailable before delivery'
    ),
    completed_at = statement_timestamp(),
    updated_at = statement_timestamp()
FROM unavailable
WHERE delivery.id = unavailable.id
  AND EXISTS (SELECT 1 FROM advance_cursor)
RETURNING delivery.id, delivery.project_id, delivery.notify_ref
`

type CancelUnavailableIntegrationDeliveriesParams struct {
	RowLimit int32
}

type CancelUnavailableIntegrationDeliveriesRow struct {
	ID        uuid.UUID
	ProjectID uuid.UUID
	NotifyRef *uuid.UUID
}

func (q *Queries) CancelUnavailableIntegrationDeliveries(ctx context.Context, arg CancelUnavailableIntegrationDeliveriesParams) ([]CancelUnavailableIntegrationDeliveriesRow, error) {
	rows, err := q.db.Query(ctx, cancelUnavailableIntegrationDeliveries, arg.RowLimit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []CancelUnavailableIntegrationDeliveriesRow{}
	for rows.Next() {
		var i CancelUnavailableIntegrationDeliveriesRow
		if err := rows.Scan(&i.ID, &i.ProjectID, &i.NotifyRef); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const claimIntegrationDeliveries = `-- name: ClaimIntegrationDeliveries :many
WITH candidate AS MATERIALIZED (
  SELECT delivery.id,
         app.configuration_revision AS app_configuration_revision,
         install.configuration_revision AS install_configuration_revision
  FROM integration_deliveries delivery
  JOIN integration_target_bindings binding
    ON binding.project_id = delivery.project_id
   AND binding.id = delivery.integration_target_binding_id
   AND binding.agent_id = delivery.agent_id
   AND binding.integration_target_id = delivery.integration_target_id
   AND binding.send_allowed
   AND binding.revoked_at IS NULL
  JOIN integration_targets target
    ON target.project_id = binding.project_id
   AND target.id = binding.integration_target_id
   AND target.deleted_at IS NULL
  JOIN integration_installs install
    ON install.project_id = binding.project_id
   AND install.id = binding.integration_install_id
   AND install.state = 'active'
   AND install.deleted_at IS NULL
  JOIN integration_apps app
    ON app.org_id = install.org_id
   AND app.id = install.integration_app_id
   AND app.state = 'active'
   AND app.deleted_at IS NULL
  WHERE delivery.transport = 'connector'
    AND delivery.connector_key = $3
    AND delivery.provider = $4
    AND delivery.state IN ('pending', 'retry_wait')
    AND delivery.available_at <= statement_timestamp()
    AND (
      binding.integration_route_id IS NULL
      OR EXISTS (
        SELECT 1 FROM integration_routes route
        WHERE route.project_id = binding.project_id
          AND route.integration_install_id = binding.integration_install_id
          AND route.id = binding.integration_route_id
          AND route.state = 'active'
          AND route.deleted_at IS NULL
      )
    )
  ORDER BY delivery.available_at, delivery.id
  FOR UPDATE OF delivery SKIP LOCKED
  LIMIT $5
)
UPDATE integration_deliveries delivery
SET state = 'claimed',
    attempt_count = delivery.attempt_count + 1,
    claim_token = uuidv7(),
    claim_generation = delivery.claim_generation + 1,
    claimed_by = $1::text,
    claimed_at = statement_timestamp(),
    claim_expires_at = statement_timestamp() +
      ($2::bigint * interval '1 microsecond'),
    updated_at = statement_timestamp()
FROM candidate
WHERE delivery.id = candidate.id
RETURNING delivery.id, delivery.project_id, delivery.agent_id,
  delivery.integration_app_id, delivery.integration_install_id,
  delivery.integration_target_id, delivery.integration_target_binding_id,
  delivery.provider, delivery.connector_key, delivery.transport,
  delivery.delivery_kind, delivery.payload_version, delivery.payload,
  delivery.idempotency_scope, delivery.idempotency_key, delivery.state,
  delivery.attempt_count, delivery.available_at, delivery.claim_token,
  delivery.claim_generation, delivery.claimed_by, delivery.claimed_at,
  delivery.claim_expires_at, delivery.notify_ref, delivery.provider_message_ref,
  delivery.last_error, delivery.completed_at, delivery.created_at, delivery.updated_at,
  candidate.app_configuration_revision, candidate.install_configuration_revision
`

type ClaimIntegrationDeliveriesParams struct {
	ClaimedBy         string
	LeaseMicroseconds int64
	ConnectorKey      string
	Provider          string
	RowLimit          int32
}

type ClaimIntegrationDeliveriesRow struct {
	ID                           uuid.UUID
	ProjectID                    uuid.UUID
	AgentID                      uuid.UUID
	IntegrationAppID             uuid.UUID
	IntegrationInstallID         uuid.UUID
	IntegrationTargetID          uuid.UUID
	IntegrationTargetBindingID   uuid.UUID
	Provider                     string
	ConnectorKey                 string
	Transport                    string
	DeliveryKind                 string
	PayloadVersion               string
	Payload                      json.RawMessage
	IdempotencyScope             string
	IdempotencyKey               string
	State                        string
	AttemptCount                 int32
	AvailableAt                  time.Time
	ClaimToken                   *uuid.UUID
	ClaimGeneration              int64
	ClaimedBy                    *string
	ClaimedAt                    *time.Time
	ClaimExpiresAt               *time.Time
	NotifyRef                    *uuid.UUID
	ProviderMessageRef           *string
	LastError                    json.RawMessage
	CompletedAt                  *time.Time
	CreatedAt                    time.Time
	UpdatedAt                    time.Time
	AppConfigurationRevision     int64
	InstallConfigurationRevision int64
}

func (q *Queries) ClaimIntegrationDeliveries(ctx context.Context, arg ClaimIntegrationDeliveriesParams) ([]ClaimIntegrationDeliveriesRow, error) {
	rows, err := q.db.Query(ctx, claimIntegrationDeliveries,
		arg.ClaimedBy,
		arg.LeaseMicroseconds,
		arg.ConnectorKey,
		arg.Provider,
		arg.RowLimit,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ClaimIntegrationDeliveriesRow{}
	for rows.Next() {
		var i ClaimIntegrationDeliveriesRow
		if err := rows.Scan(
			&i.ID,
			&i.ProjectID,
			&i.AgentID,
			&i.IntegrationAppID,
			&i.IntegrationInstallID,
			&i.IntegrationTargetID,
			&i.IntegrationTargetBindingID,
			&i.Provider,
			&i.ConnectorKey,
			&i.Transport,
			&i.DeliveryKind,
			&i.PayloadVersion,
			&i.Payload,
			&i.IdempotencyScope,
			&i.IdempotencyKey,
			&i.State,
			&i.AttemptCount,
			&i.AvailableAt,
			&i.ClaimToken,
			&i.ClaimGeneration,
			&i.ClaimedBy,
			&i.ClaimedAt,
			&i.ClaimExpiresAt,
			&i.NotifyRef,
			&i.ProviderMessageRef,
			&i.LastError,
			&i.CompletedAt,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.AppConfigurationRevision,
			&i.InstallConfigurationRevision,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const completeIntegrationDelivery = `-- name: CompleteIntegrationDelivery :one
UPDATE integration_deliveries
SET state = CASE
      WHEN $1::text = 'retry_wait'
        AND attempt_count >= $2::integer THEN 'failed'
      ELSE $1::text
    END,
    available_at = CASE
      WHEN $1::text = 'retry_wait'
        AND attempt_count < $2::integer THEN statement_timestamp() +
        ($3::bigint * interval '1 microsecond')
      ELSE available_at
    END,
    claim_token = NULL,
    claimed_by = NULL,
    claimed_at = NULL,
    claim_expires_at = NULL,
    provider_message_ref = nullif($4::text, ''),
    last_error = CASE
      WHEN $1::text = 'retry_wait'
        AND attempt_count >= $2::integer
        THEN $5::jsonb || jsonb_build_object(
          'code', 'retry_budget_exhausted',
          'message', 'delivery exhausted the core safe-retry budget'
        )
      ELSE $5::jsonb
    END,
    completed_at = CASE
      WHEN $1::text IN ('delivered', 'failed', 'unknown', 'canceled')
        OR (
          $1::text = 'retry_wait'
          AND attempt_count >= $2::integer
        )
        THEN statement_timestamp()
      ELSE NULL
    END,
    updated_at = statement_timestamp()
WHERE id = $6
  AND state = 'claimed'
  AND claim_token = $7::uuid
  AND claim_generation = $8
  AND claim_expires_at > statement_timestamp()
  AND EXISTS (
    SELECT 1
    FROM generate_subscripts($9::text[], 1) AS capability(index)
    WHERE ($9::text[])[capability.index] = integration_deliveries.connector_key
      AND ($10::text[])[capability.index] = integration_deliveries.provider
  )
RETURNING id, project_id, agent_id, integration_app_id, integration_install_id,
  integration_target_id, integration_target_binding_id, provider, connector_key,
  transport, delivery_kind, payload_version, payload, idempotency_scope,
  idempotency_key, state, attempt_count, available_at, claim_token,
  claim_generation, claimed_by, claimed_at, claim_expires_at, notify_ref,
  provider_message_ref, last_error, completed_at, created_at, updated_at
`

type CompleteIntegrationDeliveryParams struct {
	State              string
	MaxAttempts        int32
	RetryMicroseconds  int64
	ProviderMessageRef string
	LastError          json.RawMessage
	ID                 uuid.UUID
	ClaimToken         uuid.UUID
	ClaimGeneration    int64
	ConnectorKeys      []string
	Providers          []string
}

func (q *Queries) CompleteIntegrationDelivery(ctx context.Context, arg CompleteIntegrationDeliveryParams) (IntegrationDelivery, error) {
	row := q.db.QueryRow(ctx, completeIntegrationDelivery,
		arg.State,
		arg.MaxAttempts,
		arg.RetryMicroseconds,
		arg.ProviderMessageRef,
		arg.LastError,
		arg.ID,
		arg.ClaimToken,
		arg.ClaimGeneration,
		arg.ConnectorKeys,
		arg.Providers,
	)
	var i IntegrationDelivery
	err := row.Scan(
		&i.ID,
		&i.ProjectID,
		&i.AgentID,
		&i.IntegrationAppID,
		&i.IntegrationInstallID,
		&i.IntegrationTargetID,
		&i.IntegrationTargetBindingID,
		&i.Provider,
		&i.ConnectorKey,
		&i.Transport,
		&i.DeliveryKind,
		&i.PayloadVersion,
		&i.Payload,
		&i.IdempotencyScope,
		&i.IdempotencyKey,
		&i.State,
		&i.AttemptCount,
		&i.AvailableAt,
		&i.ClaimToken,
		&i.ClaimGeneration,
		&i.ClaimedBy,
		&i.ClaimedAt,
		&i.ClaimExpiresAt,
		&i.NotifyRef,
		&i.ProviderMessageRef,
		&i.LastError,
		&i.CompletedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const deleteRetainedIntegrationDeliveries = `-- name: DeleteRetainedIntegrationDeliveries :execrows
WITH expired AS (
  SELECT candidate.id
  FROM integration_deliveries candidate
  WHERE candidate.state IN ('delivered', 'failed', 'unknown', 'canceled')
    AND candidate.completed_at < statement_timestamp() -
      ($1::bigint * interval '1 microsecond')
  ORDER BY candidate.completed_at, candidate.id
  FOR UPDATE OF candidate SKIP LOCKED
  LIMIT $2
)
DELETE FROM integration_deliveries delivery
USING expired
WHERE delivery.id = expired.id
`

type DeleteRetainedIntegrationDeliveriesParams struct {
	RetentionMicroseconds int64
	RowLimit              int32
}

func (q *Queries) DeleteRetainedIntegrationDeliveries(ctx context.Context, arg DeleteRetainedIntegrationDeliveriesParams) (int64, error) {
	result, err := q.db.Exec(ctx, deleteRetainedIntegrationDeliveries, arg.RetentionMicroseconds, arg.RowLimit)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}

const expireIntegrationDeliveryClaims = `-- name: ExpireIntegrationDeliveryClaims :many
WITH expired AS MATERIALIZED (
  SELECT id
  FROM integration_deliveries
  WHERE state = 'claimed' AND claim_expires_at <= statement_timestamp()
  ORDER BY claim_expires_at, id
  FOR UPDATE SKIP LOCKED
  LIMIT $1
)
UPDATE integration_deliveries delivery
SET state = 'unknown',
    claim_token = NULL,
    claimed_by = NULL,
    claimed_at = NULL,
    claim_expires_at = NULL,
    last_error = jsonb_build_object('code', 'claim_expired', 'message', 'delivery outcome is unknown'),
    completed_at = statement_timestamp(),
    updated_at = statement_timestamp()
FROM expired
WHERE delivery.id = expired.id
RETURNING delivery.id, delivery.project_id, delivery.notify_ref
`

type ExpireIntegrationDeliveryClaimsParams struct {
	RowLimit int32
}

type ExpireIntegrationDeliveryClaimsRow struct {
	ID        uuid.UUID
	ProjectID uuid.UUID
	NotifyRef *uuid.UUID
}

func (q *Queries) ExpireIntegrationDeliveryClaims(ctx context.Context, arg ExpireIntegrationDeliveryClaimsParams) ([]ExpireIntegrationDeliveryClaimsRow, error) {
	rows, err := q.db.Query(ctx, expireIntegrationDeliveryClaims, arg.RowLimit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ExpireIntegrationDeliveryClaimsRow{}
	for rows.Next() {
		var i ExpireIntegrationDeliveryClaimsRow
		if err := rows.Scan(&i.ID, &i.ProjectID, &i.NotifyRef); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getIntegrationDelivery = `-- name: GetIntegrationDelivery :one
SELECT id, project_id, agent_id, integration_app_id, integration_install_id,
  integration_target_id, integration_target_binding_id, provider, connector_key,
  transport, delivery_kind, payload_version, payload, idempotency_scope,
  idempotency_key, state, attempt_count, available_at, claim_token,
  claim_generation, claimed_by, claimed_at, claim_expires_at, notify_ref,
  provider_message_ref, last_error, completed_at, created_at, updated_at
FROM integration_deliveries
WHERE project_id = $1 AND id = $2
`

type GetIntegrationDeliveryParams struct {
	ProjectID uuid.UUID
	ID        uuid.UUID
}

func (q *Queries) GetIntegrationDelivery(ctx context.Context, arg GetIntegrationDeliveryParams) (IntegrationDelivery, error) {
	row := q.db.QueryRow(ctx, getIntegrationDelivery, arg.ProjectID, arg.ID)
	var i IntegrationDelivery
	err := row.Scan(
		&i.ID,
		&i.ProjectID,
		&i.AgentID,
		&i.IntegrationAppID,
		&i.IntegrationInstallID,
		&i.IntegrationTargetID,
		&i.IntegrationTargetBindingID,
		&i.Provider,
		&i.ConnectorKey,
		&i.Transport,
		&i.DeliveryKind,
		&i.PayloadVersion,
		&i.Payload,
		&i.IdempotencyScope,
		&i.IdempotencyKey,
		&i.State,
		&i.AttemptCount,
		&i.AvailableAt,
		&i.ClaimToken,
		&i.ClaimGeneration,
		&i.ClaimedBy,
		&i.ClaimedAt,
		&i.ClaimExpiresAt,
		&i.NotifyRef,
		&i.ProviderMessageRef,
		&i.LastError,
		&i.CompletedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getIntegrationDeliveryByIdempotency = `-- name: GetIntegrationDeliveryByIdempotency :one
SELECT id, project_id, agent_id, integration_app_id, integration_install_id,
  integration_target_id, integration_target_binding_id, provider, connector_key,
  transport, delivery_kind, payload_version, payload, idempotency_scope,
  idempotency_key, state, attempt_count, available_at, claim_token,
  claim_generation, claimed_by, claimed_at, claim_expires_at, notify_ref,
  provider_message_ref, last_error, completed_at, created_at, updated_at
FROM integration_deliveries
WHERE project_id = $1
  AND agent_id = $2
  AND idempotency_scope = $3
  AND idempotency_key = $4
`

type GetIntegrationDeliveryByIdempotencyParams struct {
	ProjectID        uuid.UUID
	AgentID          uuid.UUID
	IdempotencyScope string
	IdempotencyKey   string
}

func (q *Queries) GetIntegrationDeliveryByIdempotency(ctx context.Context, arg GetIntegrationDeliveryByIdempotencyParams) (IntegrationDelivery, error) {
	row := q.db.QueryRow(ctx, getIntegrationDeliveryByIdempotency,
		arg.ProjectID,
		arg.AgentID,
		arg.IdempotencyScope,
		arg.IdempotencyKey,
	)
	var i IntegrationDelivery
	err := row.Scan(
		&i.ID,
		&i.ProjectID,
		&i.AgentID,
		&i.IntegrationAppID,
		&i.IntegrationInstallID,
		&i.IntegrationTargetID,
		&i.IntegrationTargetBindingID,
		&i.Provider,
		&i.ConnectorKey,
		&i.Transport,
		&i.DeliveryKind,
		&i.PayloadVersion,
		&i.Payload,
		&i.IdempotencyScope,
		&i.IdempotencyKey,
		&i.State,
		&i.AttemptCount,
		&i.AvailableAt,
		&i.ClaimToken,
		&i.ClaimGeneration,
		&i.ClaimedBy,
		&i.ClaimedAt,
		&i.ClaimExpiresAt,
		&i.NotifyRef,
		&i.ProviderMessageRef,
		&i.LastError,
		&i.CompletedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const insertIntegrationDelivery = `-- name: InsertIntegrationDelivery :one
INSERT INTO integration_deliveries(
  project_id, agent_id, integration_app_id, integration_install_id,
  integration_target_id, integration_target_binding_id,
  provider, connector_key, transport, delivery_kind, payload_version, payload,
  idempotency_scope, idempotency_key, state, available_at, notify_ref,
  created_at, updated_at
)
SELECT binding.project_id, binding.agent_id, install.integration_app_id,
       binding.integration_install_id, binding.integration_target_id, binding.id,
       install.provider, app.connector_key, $1,
       $2, $3, $4,
       $5, $6, 'pending',
       transaction_timestamp(), $7,
       transaction_timestamp(), transaction_timestamp()
FROM integration_target_bindings binding
JOIN integration_targets target
  ON target.project_id = binding.project_id
 AND target.id = binding.integration_target_id
 AND target.deleted_at IS NULL
JOIN integration_installs install
  ON install.project_id = binding.project_id
 AND install.id = binding.integration_install_id
 AND install.state = 'active'
 AND install.deleted_at IS NULL
JOIN integration_apps app
  ON app.org_id = install.org_id
 AND app.id = install.integration_app_id
 AND app.state = 'active'
 AND app.deleted_at IS NULL
WHERE binding.project_id = $8
  AND binding.agent_id = $9
  AND binding.id = $10
  AND binding.send_allowed
  AND binding.revoked_at IS NULL
  AND (
    binding.integration_route_id IS NULL
    OR EXISTS (
      SELECT 1 FROM integration_routes route
      WHERE route.project_id = binding.project_id
        AND route.integration_install_id = binding.integration_install_id
        AND route.id = binding.integration_route_id
        AND route.state = 'active'
        AND route.deleted_at IS NULL
    )
  )
ON CONFLICT (project_id, agent_id, idempotency_scope, idempotency_key) DO NOTHING
RETURNING id, project_id, agent_id, integration_app_id, integration_install_id,
  integration_target_id, integration_target_binding_id, provider, connector_key,
  transport, delivery_kind, payload_version, payload, idempotency_scope,
  idempotency_key, state, attempt_count, available_at, claim_token,
  claim_generation, claimed_by, claimed_at, claim_expires_at, notify_ref,
  provider_message_ref, last_error, completed_at, created_at, updated_at
`

type InsertIntegrationDeliveryParams struct {
	Transport                  string
	DeliveryKind               string
	PayloadVersion             string
	Payload                    json.RawMessage
	IdempotencyScope           string
	IdempotencyKey             string
	NotifyRef                  *uuid.UUID
	ProjectID                  uuid.UUID
	AgentID                    uuid.UUID
	IntegrationTargetBindingID uuid.UUID
}

func (q *Queries) InsertIntegrationDelivery(ctx context.Context, arg InsertIntegrationDeliveryParams) (IntegrationDelivery, error) {
	row := q.db.QueryRow(ctx, insertIntegrationDelivery,
		arg.Transport,
		arg.DeliveryKind,
		arg.PayloadVersion,
		arg.Payload,
		arg.IdempotencyScope,
		arg.IdempotencyKey,
		arg.NotifyRef,
		arg.ProjectID,
		arg.AgentID,
		arg.IntegrationTargetBindingID,
	)
	var i IntegrationDelivery
	err := row.Scan(
		&i.ID,
		&i.ProjectID,
		&i.AgentID,
		&i.IntegrationAppID,
		&i.IntegrationInstallID,
		&i.IntegrationTargetID,
		&i.IntegrationTargetBindingID,
		&i.Provider,
		&i.ConnectorKey,
		&i.Transport,
		&i.DeliveryKind,
		&i.PayloadVersion,
		&i.Payload,
		&i.IdempotencyScope,
		&i.IdempotencyKey,
		&i.State,
		&i.AttemptCount,
		&i.AvailableAt,
		&i.ClaimToken,
		&i.ClaimGeneration,
		&i.ClaimedBy,
		&i.ClaimedAt,
		&i.ClaimExpiresAt,
		&i.NotifyRef,
		&i.ProviderMessageRef,
		&i.LastError,
		&i.CompletedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
