// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: app_agent_conversation.sql

package dbsqlc

import (
	"context"
	"encoding/json"

	"github.com/google/uuid"
)

const insertAgentAppConversation = `-- name: InsertAgentAppConversation :execrows
INSERT INTO app_states(project_id, app_id, kind, key, data)
SELECT agent.project_id, app.id, $1, agent.id::text, $2
FROM agents agent
JOIN project_apps app ON app.project_id = agent.project_id AND app.id = $3
WHERE agent.project_id = $4 AND agent.id = $5
  AND agent.state = 'active' AND app.state = 'active' AND app.deleted_at IS NULL
`

type InsertAgentAppConversationParams struct {
	Kind      string
	Data      json.RawMessage
	AppID     uuid.UUID
	ProjectID uuid.UUID
	AgentID   uuid.UUID
}

func (q *Queries) InsertAgentAppConversation(ctx context.Context, arg InsertAgentAppConversationParams) (int64, error) {
	result, err := q.db.Exec(ctx, insertAgentAppConversation,
		arg.Kind,
		arg.Data,
		arg.AppID,
		arg.ProjectID,
		arg.AgentID,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}
