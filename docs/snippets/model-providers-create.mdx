<CodeGroup>
```bash CLI
npx omnara model-providers create \
  --name openai-prod \
  --preset openai \
  --credential-secret-id sec_hcyd5n6a2bfgik7mv4qtrwz3je \
  --json
```

```bash REST
curl "$OMNARA_API/orgs/$ORG/model-provider-configs" \
  -H "Authorization: Bearer $OMNARA_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "openai-prod",
    "preset": "openai",
    "credential_secret_id": "sec_hcyd5n6a2bfgik7mv4qtrwz3je"
  }'
```

```typescript SDK
const provider = await sdk.createModelProviderConfig({
  client,
  path: { orgID },
  body: {
    name: 'openai-prod',
    preset: 'openai',
    credential_secret_id: secret.data.id,
  },
})
```
</CodeGroup>

```json
{
  "config": {
    "id": "mpc_bfgik7mv4qtrwz3jehcyd5n6a2",
    "name": "openai-prod",
    "api_format": "openai-responses",
    "base_url": "https://api.openai.com/v1",
    "endpoint_path": "/responses",
    "credential_secret_id": "sec_hcyd5n6a2bfgik7mv4qtrwz3je",
    "...": "..."
  },
  "model_catalog": {
    "status": "ok",
    "models": [
      { "slug": "gpt-5.6", "display_name": "GPT-5.6" }
    ]
  }
}
```
