# Minimal env required (moved from code)

# API base URL for the backend
VITE_API_URL=https://agent-dashboard-backend.onrender.com
VITE_RELAY_URL=wss://relay.omnara.com/terminal

# Supabase publishable creds (frontend)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Public assets base (CDN / Storage)
# Example: Supabase Storage public bucket base URL
VITE_PUBLIC_ASSETS_BASE_URL=

# PostHog analytics
VITE_POSTHOG_KEY=
VITE_POSTHOG_HOST=https://us.i.posthog.com

# Sentry
VITE_SENTRY_DSN=
# SENTRY_AUTH_TOKEN is required for sourcemap uploads; 
# set it in your CI/build environment instead of .env files.

# Notes
# - Copy to ".env" for local development (do not commit .env)
# - Only publishable keys belong here; keep server secrets on the backend.
